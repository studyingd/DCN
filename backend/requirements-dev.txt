# Development dependencies — install with: pip install -r requirements-dev.txt
ruff>=0.11.0
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27.0       # for TestClient async support
requests>=2.31       # for E2E tests
websockets>=12.0     # for WebSocket E2E tests
